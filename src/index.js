import React, { StrictMode } from 'react';
import ReactDOM from 'react-dom/client';
import Phonebook from "./Phonebook";

ReactDOM.createRoot(document.getElementById('root')).render(
  <StrictMode><Phonebook/></StrictMode>
);
